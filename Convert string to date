import java.time.LocalDate
import java.time.format.DateTimeFormatter

fun main(args: Array<String>) {
    val string = "2022-05-01"
    val date = LocalDate.parse(string, DateTimeFormatter.ISO_DATE)

    println(date)
}
